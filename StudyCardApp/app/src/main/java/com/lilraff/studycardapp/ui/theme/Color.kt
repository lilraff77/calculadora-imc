package com.lilraff.studycardapp.ui.theme

import androidx.compose.ui.graphics.Color

// --- PALETA PRINCIPAL (GRADIENTES E FUNDOS) ---
// Azul profundo usado no fundo dos modais e nos cards de login/cadastro
val BlueGradientStart = Color(0xFF001F54)
val BlueGradientEnd = Color(0xFF00112D)

// Azul médio usado no banner "Bem-Vindo" da Home
val BannerBlue = Color(0xFF4FACFE)

// Fundo claro da tela Home (atrás dos cards brancos)
val HomeBackground = Color(0xFFF0F2F5)

// --- COMPONENTES (CARDS E CAMPOS) ---
val CardBackground = Color(0xFFFFFFFF)
val DarkBlueText = Color(0xFF002B5C)
val PlaceholderGray = Color(0xFF8E8E8E)

// --- AÇÕES E ALERTAS ---
// Azul escuro dos botões principais
val ButtonBlue = Color(0xFF002B5C)

// Vermelho para botões de "Sair", "Excluir" e bordas de "Confirmar"
val RedAction = Color(0xFFB71C1C)

// Cores padrões do Material Theme
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)